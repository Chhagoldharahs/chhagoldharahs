# ছাগলধরা উচ্চ বিদ্যালয় ওয়েবসাইট — Cloudflare Pages সেটআপ গাইড

এই zip-টা Netlify থেকে Cloudflare Pages-এ সম্পূর্ণ মাইগ্রেট করা। নিচের ধাপগুলো
একে একে করলেই সাইট আগের মতোই সব ফিচারসহ Cloudflare-এ চালু হয়ে যাবে।

## ধাপ ১: Cloudflare একাউন্ট ও Pages প্রজেক্ট বানানো

1. https://dash.cloudflare.com -এ গিয়ে ফ্রি একাউন্ট খুলুন (না থাকলে)
2. বাম মেনু থেকে **Workers & Pages** > **Create application** > **Pages** ট্যাব
3. দুইভাবে ডিপ্লয় করা যায়:
   - **Direct Upload** (সহজ): "Upload assets" বেছে এই পুরো zip-এর ভেতরের সব ফাইল আপলোড করুন
   - **GitHub দিয়ে** (ভবিষ্যতে সহজে আপডেট করতে চাইলে ভালো): আগে এই ফাইলগুলো একটা GitHub রিপোতে রাখুন, তারপর "Connect to Git" দিয়ে সংযুক্ত করুন
4. Build settings-এ কিছু বদলাতে হবে না (এটা স্ট্যাটিক সাইট + Functions, বিল্ড কমান্ড খালি রাখুন, Build output directory: `/`)

## ধাপ ২: KV Namespace বানানো (Netlify Blobs-এর বদলে)

1. Cloudflare ড্যাশবোর্ডে **Workers & Pages** > **KV** > **Create a namespace**
2. নাম দিন: `SCHOOL_KV` (বা যেকোনো নাম, পরের ধাপে বাইন্ড করার সময় নাম মিলিয়ে দেবেন)
3. আপনার Pages প্রজেক্টে যান > **Settings** > **Functions** > **KV namespace bindings** > **Add binding**
   - Variable name: `SCHOOL_KV` (ঠিক এই নামেই দিতে হবে, কোডে এই নামেই রেফার করা আছে)
   - KV namespace: উপরে বানানো namespace বেছে নিন
4. সেভ করার পর **নতুন করে ডিপ্লয়** করুন (বাইন্ডিং পরিবর্তনের পর রি-ডিপ্লয় লাগে)

## ধাপ ৩: Environment Variables সেট করা

Pages প্রজেক্ট > **Settings** > **Environment variables** > **Add variable** — এই ভ্যারিয়েবলগুলো (Production ও Preview দুই জায়গাতেই) বসান, ঠিক আগে Netlify-তে যা ছিল তাই:

| নাম | মান |
|---|---|
| `GAS_WEBAPP_URL` | আপনার Google Apps Script ওয়েব অ্যাপের URL |
| `SHEET_API_KEY` | GAS স্ক্রিপ্টে সেট করা SHEET_API_KEY-এর একই মান |
| `ADMIN_PASSWORD` | admin.html-এর লগইন পাসওয়ার্ড |
| `TOTP_SECRET` | 2FA সেটআপ করা থাকলে সেই সিক্রেট (ঐচ্ছিক) |
| `VAPID_PUBLIC_KEY` | পুশ নোটিফিকেশনের পাবলিক কী (নিচে ⚠️ নোট দেখুন) |
| `VAPID_PRIVATE_KEY` | পুশ নোটিফিকেশনের প্রাইভেট কী (নিচে ⚠️ নোট দেখুন) |
| `VAPID_SUBJECT` | `mailto:chhagoldharahighschool.info@gmail.com` |
| `DEPLOY_NOTIFY_SECRET` | ঐচ্ছিক — "সাইট আপডেট হয়েছে" নোটিফিকেশনের জন্য একটা গোপন স্ট্রিং |

**`BLOBS_SITE_ID` ও `BLOBS_TOKEN` (Netlify-তে ছিল) এখন লাগবে না** — KV বাইন্ডিং দিয়েই কাজ হয়ে যায়।

## ⚠️ গুরুত্বপূর্ণ: পুশ নোটিফিকেশন নিয়ে বিশেষ সতর্কতা

আগে `web-push` (Node.js লাইব্রেরি) ব্যবহার হতো, যেটা Cloudflare Workers-এ চলে না
(এটা Node-এর `crypto`/`https` মডিউলের ওপর নির্ভরশীল)। তাই এখানে
**`@pushforge/builder`** নামে একটা Workers-উপযোগী লাইব্রেরি বসানো হয়েছে।

**এটা প্রথমেই টেস্ট করে দেখুন** (admin.html থেকে একটা টেস্ট নোটিশ পাঠিয়ে):
- **যদি কাজ করে** → কিছু করতে হবে না, আগের VAPID কী-ই ব্যবহার করতে থাকুন
- **যদি কাজ না করে** (এরর আসে বা নোটিফিকেশন না পৌঁছায়) → নতুন VAPID কী বানাতে হবে:
  1. https://vapidkeys.com এ গিয়ে নতুন এক জোড়া কী জেনারেট করুন
  2. Cloudflare-এ `VAPID_PUBLIC_KEY` ও `VAPID_PRIVATE_KEY` আপডেট করুন
  3. `school.html`-এর **৪৩৭৫ নং লাইনে** `const VAPID_PUBLIC_KEY = '...'` — এই মানটাই নতুন public key দিয়ে বদলাতে হবে (এই অংশটা খুঁজে বদলাতে বললে আমি করে দেব)
  4. **আগে থেকে সাবস্ক্রাইব করা সবাইকে একবার আবার "নোটিফিকেশন চালু করুন" চাপতে হবে** (এটা একবারই লাগবে, পুরনো সাবস্ক্রিপশন নতুন কী দিয়ে কাজ করবে না)

## ধাপ ৪: Service Worker ক্যাশ ভার্সন

`sw.js`-এ `CACHE_NAME` আগের ভার্সন থেকে বাড়িয়ে দিন (যেমন `v9` থেকে `v10`), যাতে
ব্যবহারকারীদের ব্রাউজারে পুরনো ক্যাশ হওয়া `/.netlify/functions/` পাথ স্বয়ংক্রিয়ভাবে
মুছে গিয়ে নতুন `/api/` পাথ ব্যবহার হয়।

## ধাপ ৫: টেস্ট চেকলিস্ট (ডিপ্লয়ের পর যাচাই করুন)

- [ ] হোমপেজ/school.html ঠিকভাবে লোড হচ্ছে
- [ ] রুটিন, শিক্ষক তালিকা, ইভেন্ট, বোর্ড রেজাল্ট ইত্যাদি (Google Sheet ডেটা) দেখাচ্ছে
- [ ] admin.html-এ লগইন করা যাচ্ছে
- [ ] নতুন নোটিশ প্রকাশ করা যাচ্ছে ও ওয়েবসাইটে দেখাচ্ছে
- [ ] নোটিশ ডিলিট করা যাচ্ছে
- [ ] অভিযোগ ফর্ম জমা দেওয়া যাচ্ছে
- [ ] জরুরি রক্তদান অ্যালার্ট পাঠানো ও ইতিহাস দেখা যাচ্ছে
- [ ] মতামত জরিপে ভোট দেওয়া ও ফলাফল দেখা যাচ্ছে
- [ ] পুশ নোটিফিকেশন (উপরের ⚠️ নোট অনুযায়ী)

## যা একদম বদলায়নি

- Google Apps Script (Code.gs) — Google-এ হোস্ট করা, কোনো পরিবর্তন লাগবে না
- Google Sheet-এর সব ট্যাব ও ডেটা
- school.html, admin.html-এর ডিজাইন ও ফিচার — সব হুবহু আগের মতো
